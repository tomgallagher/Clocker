import React from 'react';
import { useStores } from './../../../hooks/useStores';

export const LoadCustomList = () => {
    const { Settings } = useStores();

    return <div>TO DO: Load custom lists</div>;
};
